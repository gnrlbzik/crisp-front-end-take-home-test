{
  "extends": ["react-app"],
  "rules": {},
  "overrides": [
    {
      "files": ["**/*.ts?(x)"],
      "rules": {
        "semi": ["error", "always"],
        "quotes": ["error", "single", { "allowTemplateLiterals": true }]
      }
    }
  ]
}